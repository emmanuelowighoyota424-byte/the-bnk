'use client';
import {useEffect,useState} from 'react'; import {useRouter} from 'next/navigation'; import BankNav from '../../components/BankNav';
export default function Security(){const [p,setP]=useState<any>(null);const router=useRouter();useEffect(()=>{fetch('/api/v1/profile').then(r=>r.json()).then(x=>setP(x.data||null))},[]);async function logout(){await fetch('/api/v1/auth/logout',{method:'POST'});router.replace('/login')}return <><BankNav/><main className="mx-auto max-w-3xl px-4 py-8"><h1 className="text-3xl font-bold">Security</h1><p className="mt-1 text-slate-500">Review your authentication status and active session controls.</p><div className="mt-6 space-y-4"><div className="rounded-2xl bg-white p-6 ring-1 ring-slate-200"><h2 className="font-semibold">Authentication</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">Two-factor authentication</p><p className="mt-1 font-semibold">{p?.totpEnabled?'Enabled':'Not enabled'}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">Account status</p><p className="mt-1 font-semibold">{p?.status||'Loading…'}</p></div></div></div><div className="rounded-2xl bg-white p-6 ring-1 ring-slate-200"><h2 className="font-semibold">Sign out</h2><p className="mt-1 text-sm text-slate-500">End the current browser session.</p><button onClick={logout} className="mt-4 rounded-xl border border-red-200 px-5 py-3 font-semibold text-red-700">Sign out</button></div></div></main></>}
