'use client'; import {useEffect,useState} from 'react'; import BankNav from '../../components/BankNav'; export default function Deposit(){const [a,setA]=useState<any[]>([]),[accountId,setAccountId]=useState(''),[amount,setAmount]=useState(''),[msg,setMsg]=useState('');useEffect(()=>{fetch('/api/v1/accounts').then(r=>r.json()).then(x=>setA(x.data||[]))},[]);async function go(e:any){e.preventDefault();const r=await fetch('/api/v1/deposits',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({accountId,amount})});const x=await r.json();setMsg(x.success?'Deposit '+x.data.reference+' submitted and is pending.':x.error||'Deposit failed')}return <><BankNav/><main className="mx-auto max-w-2xl px-4 py-10"><h1 className="text-3xl font-bold">Deposit</h1><form onSubmit={go} className="mt-8 space-y-5 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><select required value={accountId} onChange={e=>setAccountId(e.target.value)} className="w-full rounded-xl border p-3"><option value="">Destination account</option>{a.map(x=><option key={x.id} value={x.id}>••••{x.accountNumber.slice(-4)}</option>)}</select><input required placeholder="Amount" value={amount} onChange={e=>setAmount(e.target.value)} className="w-full rounded-xl border p-3"/><button className="w-full rounded-xl bg-indigo-600 p-3 font-semibold text-white">Submit deposit</button>{msg&&<p className="rounded-xl bg-slate-50 p-3">{msg}</p>}</form></main></>}