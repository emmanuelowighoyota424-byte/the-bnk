'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
type Withdrawal={id:string;amount:string|number;currency:string;reference:string;status:string;destination:string;createdAt:string;userId:string;accountId:string};
export default function AdminWithdrawalsPage(){const [items,setItems]=useState<Withdrawal[]>([]);const [loading,setLoading]=useState(true);const [busy,setBusy]=useState<string|null>(null);const load=async()=>{setLoading(true);try{const r=await fetch('/api/v1/admin/withdrawals');const j=await r.json();if(j.success)setItems(j.data)}finally{setLoading(false)}};useEffect(()=>{load()},[]);const act=async(id:string,action:'approve'|'reject')=>{setBusy(id);try{const r=await fetch(`/api/v1/admin/withdrawals/${id}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action})});const j=await r.json();if(!j.success)alert(j.error||'Operation failed');await load()}finally{setBusy(null)}};return <div className="min-h-screen bg-gray-950 text-white"><header className="border-b border-gray-800 bg-gray-900"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link href="/admin/dashboard" className="font-bold text-xl">Crestline Capital · Admin</Link><nav className="flex gap-4 text-sm text-gray-400"><Link href="/admin/dashboard">Dashboard</Link><Link href="/admin/deposits">Deposits</Link><span className="text-white">Withdrawals</span><Link href="/admin/users">Users</Link></nav></div></header><main className="mx-auto max-w-7xl px-4 py-8"><h1 className="text-2xl font-semibold">Withdrawal Review</h1><p className="mt-1 mb-6 text-gray-400">Approve or reject pending withdrawal requests.</p>{loading?<div className="py-16 text-center text-gray-400">Loading withdrawals…</div>:<div className="overflow-x-auto rounded-xl border border-gray-800 bg-gray-900"><table className="w-full text-sm"><thead className="border-b border-gray-800 text-left text-gray-400"><tr><th className="p-4">Reference</th><th className="p-4">Amount</th><th className="p-4">Destination</th><th className="p-4">Status</th><th className="p-4">Requested</th><th className="p-4">Action</th></tr></thead><tbody className="divide-y divide-gray-800">{items.map(w=><tr key={w.id}><td className="p-4 font-mono text-xs">{w.reference}</td><td className="p-4">${Number(w.amount).toFixed(2)} {w.currency}</td><td className="p-4 max-w-xs truncate">{w.destination}</td><td className="p-4"><span className="rounded-full bg-gray-800 px-2.5 py-1 text-xs">{w.status}</span></td><td className="p-4 text-gray-400">{new Date(w.createdAt).toLocaleString()}</td><td className="p-4">{w.status==='pending'?<div className="flex gap-2"><button disabled={busy===w.id} onClick={()=>act(w.id,'approve')} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs disabled:opacity-50">Approve</button><button disabled={busy===w.id} onClick={()=>act(w.id,'reject')} className="rounded-lg bg-red-600 px-3 py-2 text-xs disabled:opacity-50">Reject</button></div>:<span className="text-gray-500">No action</span>}</td></tr>)}{items.length===0&&<tr><td colSpan={6} className="p-12 text-center text-gray-500">No withdrawals found.</td></tr>}</tbody></table></div>}</main></div>}
