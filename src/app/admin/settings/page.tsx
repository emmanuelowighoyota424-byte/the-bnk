'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link'; export default function Settings(){const [s,setS]=useState<any>(null);useEffect(()=>{fetch('/api/v1/admin/settings').then(r=>r.json()).then(x=>setS(x.data||null))},[]);return <main className="mx-auto max-w-4xl px-4 py-8"><Link href="/admin/dashboard" className="text-sm text-indigo-600">← Admin</Link><h1 className="mt-2 text-3xl font-bold">Settings</h1><p className="mt-1 text-slate-500">Operational configuration status. Secret values are never displayed.</p><div className="mt-6 rounded-2xl bg-white p-6 ring-1 ring-slate-200">{!s?<p>Loading…</p>:<div className="grid gap-3 sm:grid-cols-2">{Object.entries(s).map(([k,v])=><div key={k} className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase text-slate-500">{k}</p><p className="mt-1 font-semibold">{typeof v==='boolean'?(v?'Configured':'Missing'):String(v)}</p></div>)}</div>}</div></main>}
