'use client';

import { useCallback, useEffect, useState } from 'react';

interface User { id:string; email:string; firstName:string; lastName:string; bnkTag:string|null; kycStatus:string; kycTier:number; status:string; createdAt:string; _count:{accounts:number} }

export default function AdminUsersPage(){
 const [users,setUsers]=useState<User[]>([]),[search,setSearch]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState(''),[page,setPage]=useState(1),[totalPages,setTotalPages]=useState(1);
 const fetchUsers=useCallback(async(q:string,p:number)=>{setLoading(true);setError('');try{const params=new URLSearchParams({page:String(p),limit:'20'});if(q)params.set('search',q);const r=await fetch(`/api/v1/admin/users?${params}`,{cache:'no-store'});const d=await r.json();if(!r.ok||!d.success)throw new Error(d.error||'Unable to load users');setUsers(d.data.users||[]);setTotalPages(d.data.pagination?.totalPages||1)}catch(e){setError(e instanceof Error?e.message:'Unable to load users')}finally{setLoading(false)}},[]);
 useEffect(()=>{fetchUsers(search,page)},[fetchUsers,search,page]);
 return <main className="space-y-6 pl-0 sm:pl-2"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div className="pl-10 sm:pl-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-indigo-400">Overview & People</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-white">Users</h1><p className="mt-2 text-sm text-slate-400">Customer master roster with server-side search and pagination.</p></div><form onSubmit={e=>{e.preventDefault();setPage(1)}} className="flex gap-2"><input aria-label="Search users" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Name, email or tag" className="min-h-11 w-full rounded-xl border border-white/10 bg-slate-900 px-4 text-sm text-white outline-none placeholder:text-slate-600 focus:border-indigo-400 sm:w-72"/><button className="rounded-xl bg-indigo-600 px-4 text-sm font-semibold text-white">Search</button></form></div>{error&&<div role="alert" className="rounded-xl border border-red-400/20 bg-red-400/10 px-4 py-3 text-sm text-red-200">{error}</div>}<section className="overflow-hidden rounded-2xl border border-white/10 bg-slate-900/80"><div className="overflow-x-auto"><table className="min-w-[900px] w-full text-left"><thead><tr className="border-b border-white/10">{['Customer','Tag','KYC','Status','Accounts','Joined'].map(x=><th key={x} className="px-5 py-3 text-[11px] font-bold uppercase tracking-wider text-slate-500">{x}</th>)}</tr></thead><tbody>{loading?[1,2,3,4,5].map(n=><tr key={n}><td colSpan={6} className="px-5 py-4"><div className="h-10 animate-pulse rounded-lg bg-white/[.04]"/></td></tr>):users.map(u=><tr key={u.id} className="border-b border-white/5 hover:bg-white/[.025]"><td className="px-5 py-4"><p className="font-semibold text-white">{u.firstName} {u.lastName}</p><p className="text-xs text-slate-500">{u.email}</p></td><td className="px-5 py-4 text-sm text-slate-300">{u.bnkTag||'—'}</td><td className="px-5 py-4"><span className="rounded-full bg-indigo-400/10 px-2.5 py-1 text-xs text-indigo-300">Tier {u.kycTier} · {u.kycStatus}</span></td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs ${u.status==='active'?'bg-emerald-400/10 text-emerald-300':'bg-red-400/10 text-red-300'}`}>{u.status}</span></td><td className="px-5 py-4 text-sm text-slate-300">{u._count.accounts}</td><td className="px-5 py-4 text-sm text-slate-500">{new Date(u.createdAt).toLocaleDateString()}</td></tr>)}{!loading&&users.length===0&&<tr><td colSpan={6} className="px-5 py-14 text-center text-sm text-slate-500">No data available</td></tr>}</tbody></table></div>{totalPages>1&&<div className="flex items-center justify-center gap-4 border-t border-white/10 p-4"><button disabled={page===1} onClick={()=>setPage(p=>Math.max(1,p-1))} className="rounded-lg border border-white/10 px-4 py-2 text-sm text-slate-300 disabled:opacity-40">Previous</button><span className="text-sm text-slate-500">Page {page} of {totalPages}</span><button disabled={page===totalPages} onClick={()=>setPage(p=>Math.min(totalPages,p+1))} className="rounded-lg border border-white/10 px-4 py-2 text-sm text-slate-300 disabled:opacity-40">Next</button></div>}</section></main>;
}
