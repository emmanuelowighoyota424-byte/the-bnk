'use client';
import {useEffect,useState} from 'react';
type Card={id:string;lastFour:string;cardNetwork:string;status:string;user:{email:string;firstName:string;lastName:string};account:{accountNumber:string}};
export default function AdminCardsPage(){const[cards,setCards]=useState<Card[]>([]);const[error,setError]=useState('');useEffect(()=>{fetch('/api/v1/admin/cards').then(r=>r.json()).then(x=>{if(!x.success)throw new Error(x.error||'Unable to load cards');setCards(x.data||[])}).catch(e=>setError(e.message))},[]);return <main className="mx-auto max-w-7xl px-4 py-8"><h1 className="text-3xl font-bold">Crestline Capital cards</h1><p className="mt-2 text-sm text-slate-500">Card inventory and customer ownership.</p>{error&&<p className="mt-4 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="mt-7 overflow-x-auto rounded-2xl bg-white ring-1 ring-slate-200"><table className="min-w-full text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-4">Customer</th><th className="p-4">Card</th><th className="p-4">Account</th><th className="p-4">Status</th></tr></thead><tbody>{cards.map(c=><tr key={c.id} className="border-b last:border-0"><td className="p-4"><p className="font-semibold">{c.user.firstName} {c.user.lastName}</p><p className="text-xs text-slate-500">{c.user.email}</p></td><td className="p-4">•••• {c.lastFour} · {c.cardNetwork}</td><td className="p-4">•••• {c.account.accountNumber.slice(-4)}</td><td className="p-4">{c.status}</td></tr>)}</tbody></table></div></main>}
