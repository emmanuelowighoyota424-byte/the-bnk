'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
import BankNav from '../../components/BankNav';
type Card={id:string;cardNetwork:string;lastFour:string;expiryMonth:number;expiryYear:number;status:string;isVirtual:boolean;account:{accountNumber:string}};
export default function CardsPage(){const[cards,setCards]=useState<Card[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/v1/cards').then(r=>r.json()).then(x=>setCards(x.data||[])).finally(()=>setLoading(false))},[]);return <><BankNav/><main className="min-h-[calc(100vh-4rem)] bg-slate-50 px-4 py-8"><div className="mx-auto max-w-6xl"><p className="text-sm font-semibold text-indigo-700">Crestline Capital</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Cards</h1><p className="mt-2 text-sm text-slate-500">View your Crestline Capital cards and linked accounts.</p>{loading?<div className="mt-8 h-64 animate-pulse rounded-3xl bg-white"/>:cards.length===0?<div className="mt-8 rounded-3xl bg-white p-12 text-center ring-1 ring-slate-200"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-slate-950 text-xl font-bold text-white">C</div><h2 className="mt-5 text-xl font-bold">No cards yet</h2><p className="mt-2 text-sm text-slate-500">Cards issued to your Crestline Capital account will appear here.</p></div>:<div className="mt-8 grid gap-5 md:grid-cols-2">{cards.map(c=><article key={c.id} className="overflow-hidden rounded-3xl bg-slate-950 p-7 text-white shadow-xl"><div className="flex justify-between"><span className="font-semibold">Crestline Capital</span><span className="text-xs uppercase tracking-widest text-slate-400">{c.cardNetwork}</span></div><div className="mt-12 text-2xl tracking-[.3em]">•••• •••• •••• {c.lastFour}</div><div className="mt-10 flex items-end justify-between"><div><p className="text-[10px] uppercase text-slate-400">Expires</p><p className="font-semibold">{String(c.expiryMonth).padStart(2,'0')}/{String(c.expiryYear).slice(-2)}</p></div><span className="rounded-full bg-white/10 px-3 py-1 text-xs">{c.isVirtual?'Virtual':'Physical'} · {c.status}</span></div><p className="mt-5 text-xs text-slate-400">Linked account •••• {c.account.accountNumber.slice(-4)}</p></article>)}</div>}</div></main></>}
