'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {useParams} from 'next/navigation'; import BankNav from '@/components/BankNav';
export default function AccountDetailsPage(){const params=useParams<{id:string}>();const [account,setAccount]=useState<any>(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{fetch(`/api/v1/accounts/${encodeURIComponent(params.id)}`).then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error||'Account not found');return x}).then(x=>setAccount(x.data)).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[params.id]);if(loading)return <><BankNav/><main className="mx-auto max-w-5xl px-4 py-10">Loading account…</main></>;if(error||!account)return <><BankNav/><main className="mx-auto max-w-5xl px-4 py-10"><div className="rounded-2xl bg-white p-8 ring-1 ring-slate-200"><h1 className="text-xl font-bold">Account unavailable</h1><p className="mt-2 text-slate-500">{error||'Account not found.'}</p><Link className="mt-5 inline-block text-indigo-600" href="/accounts">Back to accounts</Link></div></main></>;const money=(v:any)=>new Intl.NumberFormat('en-US',{style:'currency',currency:account.currency}).format(Number(v||0));return <><BankNav/><main className="mx-auto max-w-6xl px-4 py-8"><Link href="/accounts" className="text-sm text-indigo-600">← Accounts</Link><section className="mt-4 rounded-3xl bg-slate-900 p-7 text-white"><div className="flex flex-wrap justify-between gap-4"><div><p className="text-sm text-slate-300">{String(account.accountType).replace(/_/g,' ')}</p><p className="mt-2 text-4xl font-bold">{money(account.balance)}</p><p className="mt-2 text-sm text-slate-300">Available {money(account.availableBalance)}</p></div><div className="text-right text-sm"><p>•••• {account.accountNumber.slice(-4)}</p><p className="mt-1 text-slate-300">{account.currency} · {account.status}</p></div></div></section><section className="mt-7 rounded-2xl bg-white p-6 ring-1 ring-slate-200"><h2 className="text-lg font-semibold">Recent activity</h2><div className="mt-4 divide-y">{(account.transactions||[]).map((t:any)=><div key={t.id} className="flex items-center justify-between gap-4 py-4"><div><p className="font-medium text-slate-800">{t.description||t.txType}</p><p className="text-xs text-slate-500">{new Date(t.createdAt).toLocaleString()} · {t.referenceId||'No reference'}</p></div><div className="text-right"><p className="font-semibold">{money(t.amount)}</p><p className="text-xs text-slate-500">{t.status}</p></div></div>)}{!(account.transactions||[]).length&&<p className="py-6 text-slate-500">No transactions yet.</p>}</div></section></main></>}
