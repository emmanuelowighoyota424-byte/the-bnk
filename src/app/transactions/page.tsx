'use client'; import {useEffect,useState} from 'react'; import BankNav from '@/components/BankNav'; export default function Transactions(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch('/api/v1/transactions?limit=100').then(r=>r.json()).then(x=>setItems(x.data?.items||[]))},[]);return <><BankNav/><main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-3xl font-bold">Transactions</h1><div className="mt-8 overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200">{items.map(t=><div key={t.id} className="flex items-center justify-between border-b p-5"><div><p className="font-semibold">{t.description||t.txType}</p><p className="text-xs text-slate-500">{t.referenceId||t.id} · {new Date(t.createdAt).toLocaleString()}</p></div><div className="text-right"><p className="font-semibold">${Number(t.amount).toFixed(2)}</p><p className="text-xs uppercase text-slate-500">{t.status}</p></div></div>)}{!items.length&&<p className="p-10 text-center text-slate-500">No transactions yet.</p>}</div></main></>}